In Python, data types define the kind of value a variable can hold. Here are the different types of data types in Python:

1. Numeric Types

  int → Integer (e.g., 10, -5, 1000)
  float → Floating-point numbers (e.g., 10.5, -3.14, 2.0)
  complex → Complex numbers (e.g., 2 + 3j, -1j)

2. Sequence Types

  str → String (e.g., "Hello", 'Python')
  list → Ordered, mutable collection (e.g., [1, 2, 3], ["a", "b", "c"])
  tuple → Ordered, immutable collection (e.g., (1, 2, 3), ("x", "y", "z"))
  range → Sequence of numbers (e.g., range(5) → 0, 1, 2, 3, 4)

3. Set Types

  set → Unordered, unique elements (e.g., {1, 2, 3}, {"apple", "banana"})
  frozenset → Immutable set (e.g., frozenset({1, 2, 3}))

4. Mapping Type

  dict → Key-value pairs (e.g., {"name": "Alice", "age": 25})

5. Boolean Type

  bool → Boolean values (True, False)

6. Binary Types

  bytes → Immutable sequence of bytes (e.g., b'hello')
  bytearray → Mutable sequence of bytes (e.g., bytearray([65, 66, 67]))
  memoryview → View of a bytes object (e.g., memoryview(b'abc'))

7. None Type

  NoneType → Represents the absence of a value (None)

Each of these data types plays a crucial role in Python programming.
